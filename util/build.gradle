plugins {
    id 'java'
}

group = 'com.crush'
version = project.properties['crush.version']

dependencies {
    // 内部引用
    implementation project(':common')

    compileOnly 'ch.qos.logback:logback-core'
    compileOnly 'ch.qos.logback:logback-classic'
}

test {
    useJUnitPlatform()
}