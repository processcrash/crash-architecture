plugins {
    id 'java'
}

group = 'com.crush.starter'
version = project.properties['crush.version']

dependencies {
    // 内部依赖
    implementation project(':common')

    implementation 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
}

test {
    useJUnitPlatform()
}